# Ponytail audit — Hermes

Date: 2026-08-08. Coverage: Python agent core (root files, `hermes_cli/`, `agent/`, `gateway/`) — ~500k lines sampled strategically by 3 parallel auditors + a dependency pass. All paths relative to repo root. Every dead-code claim verified by repo-wide grep (node_modules/.worktrees excluded). Ranked by payoff = lines cut × hotness. Out of scope: correctness, security, perf.

## Findings

1. `shrink` cli.py `process_command`: 699-line, 94-branch elif chain on `canonical`. Add a `handler` field to the existing 94-entry `COMMAND_REGISTRY` in hermes_cli/commands.py (gateway/run.py:14340 already dispatches these via dict). [cli.py:9845] (~660 lines, hot)
2. `shrink` `run_doctor`: one 2,078-line function, 17 sections all shaped try/lazy-import/probe/check-or-warn. `[(section, label, probe_fn)]` table + ~15-line driver; the 6 extracted `_check_*` fns show the pattern. [hermes_cli/doctor.py:708-2785] (~700 lines, warm)
3. `yagni` Micro-compaction — a second, dark-by-default compression algorithm (`micro_compact: False`) inside ContextCompressor: 15 methods + gates + report script. Batch `compress()` already covers it. [agent/context_compressor.py:5409-6161, agent/turn_finalizer.py:360-410, agent/agent_init.py:2051-2067, scripts/micro_compaction_report.py] (~1,010 lines, cold)
4. `shrink` ~52 AIAgent forwarder methods whose whole body is `from agent.X import y; return y(self, ...)` plus 19 `# noqa: F401` re-exports kept as mock-patch seams. Fold agent/* helpers in as mixins, call directly. [run_agent.py:512-4831, run_agent.py:113-221] (~350 lines, hot)
5. `delete` `mini_swe_runner.py` + tests/test_mini_swe_runner.py — standalone SWE harness with zero references repo-wide (code, docs, entry points). batch_runner.py is the supported path. [mini_swe_runner.py:1-732] (~790 lines, cold)
6. `delete` God-file compat ring: main.py `# noqa: F401` re-export blocks, update_cmd's 166 `_m().` indirections, model_setup_flows' 12 call-time `from hermes_cli.main import`, 12 one-line `*_fast` wrappers — all kept so old `hermes_cli.main.<name>` monkeypatches resolve. Retarget test patches to the new modules; import directly. [hermes_cli/main.py:5045,7264,374-414; hermes_cli/update_cmd.py:45; hermes_cli/model_setup_flows.py; hermes_cli/sessions_cmd.py:27] (~250 lines, hot)
7. `shrink` Five concurrency mechanisms guarding one `compress()` call — CommitFence (13 methods, 4 reached), admission/executor, progress-timeout wrapper, heartbeat, lock-lease refresher. One lock + one cancel flag + one timeout; needs a design pass, not extraction. [agent/conversation_compression.py:445-1090,1406-1596] (~300 lines, warm)
8. `shrink` models.py provider-fetch sprawl: 17 flat `if normalized == "<provider>"` blocks over ~10 variants of "GET /models, parse data[], pull id", plus two divergent `fetch_ai_gateway_models` implementations. One `{slug: fetcher}` table + one generic catalog fetcher. [hermes_cli/models.py:2821, 1504-1686, 4600] (~300 lines, warm)
9. `yagni` `TTSProvider` ABC + registry — zero concrete implementations repo-wide (built-ins live in `tools/tts_tool.BUILTIN_TTS_PROVIDERS`). Membership test against the built-in list. Documented public extension point, so product call. [agent/tts_provider.py, agent/tts_registry.py, hermes_cli/plugins.py:897-930] (~500 lines, cold)
10. `yagni` `TranscriptionProvider` ABC + registry — same shape, zero implementations. Same replacement, same caveat. [agent/transcription_provider.py, agent/transcription_registry.py] (~400 lines, cold)
11. `shrink` Three near-duplicate RFC 8628 device-code flows (Nous, xAI, Copilot — the Copilot one also hand-builds urllib HTTP with httpx installed). One parameterized ~90-line flow. [hermes_cli/auth.py:5130-8005, hermes_cli/copilot_auth.py:200-305] (~200 lines, warm)
12. `shrink` Four parallel fence-aware markdown chunkers — `base.truncate_message`, three helpers.py functions with one caller each, yuanbao's `chunk_markdown_text`. One splitter with options. [gateway/platforms/base.py:6702, gateway/platforms/helpers.py:544-950, gateway/platforms/yuanbao.py:4710] (~200 lines, warm)
13. `shrink` `TurnContext` — 60-field dataclass + 41-kwarg construction whose only job is handing TurnRunner the caller's locals. Pass the runner + source. [gateway/turn_context.py:33-131, gateway/run.py:25038-25078] (~100 lines, hot)
14. `stdlib` Six hand-rolled TTL/disk cache layers in one module (six TTL constants, six get/put families). One small TTL-dict helper + existing `agent/secret_sources/_cache.DiskCache`. [agent/model_metadata.py:134-400] (~150 lines, warm)
15. `shrink` Three independent systemd-restart implementations (scope detect, sudo -n fallback, is-active polling). One helper in gateway.py. [hermes_cli/gateway.py:3481,1108; hermes_cli/update_cmd.py:4700-5200; hermes_cli/main.py:7345,7503] (~150 lines, warm)
16. `shrink` Bool/env parsing re-rolled everywhere: `env_float` clones ×4, `_coerce_bool`/`_as_bool`/`_truthy_env` ×5, ~45 inline `in ("1","true",...)` checks — root utils.py already has `env_float/env_int/env_bool/is_truthy_value`. [gateway/run.py:1006, gateway/platforms/base.py:56, gateway/platforms/_http_client_limits.py:53-63, agent/chat_completion_helpers.py:224, gateway/config.py:26, agent/tool_guardrails.py:575, +] (~130 lines, mixed)
17. `shrink` 50-branch slash-command elif chain of `if canonical == "x": return await self._handle_x_command(event)` — the busy path 1,000 lines up already uses a dict for the same job. [gateway/run.py:15298-15620] (~95 lines, hot)
18. `delete` `plugins/context_engine/` discovery+loader scanning a directory containing only `__init__.py`; the general plugin system already covers the fallthrough. [plugins/context_engine/__init__.py:1-285, agent/agent_init.py:2413-2419] (~295 lines, cold)
19. `delete` `trajectory_compressor.py` sync path (`compress_trajectory`, `process_entry`, `_generate_summary`) — zero external callers; production uses the ~90%-identical async twins; also drop the never-read `num_workers`/`save_over_limit` config knobs. [trajectory_compressor.py:605-1064,114-178] (~280 lines, cold)
20. `delete` `session_recap.py` — referenced only by its own test; no CLI/gateway/TUI wiring; `/resume` recap is a separate implementation. [hermes_cli/session_recap.py:1-322] (~322 lines, cold)
21. `delete` `gateway/memory_monitor.py` — `start_memory_monitoring` has zero production callers (tests only); its config block is documented but never wired. [gateway/memory_monitor.py:1-230] (~230 lines, cold)
22. `native` ~16 hand-rolled tmp-file + `os.replace` atomic writes across gateway/, agent/, hermes_cli/, cli.py — several silently missing the fsync/mode-preservation the canonical helpers have. `utils.atomic_write_text`/`atomic_json_write` (root utils.py:139/206). [gateway/status.py:116, gateway/rich_sent_store.py:66, gateway/sticker_cache.py:50, agent/anthropic_adapter.py:1269, hermes_cli/active_sessions.py:197, hermes_cli/models.py:942, hermes_cli/copilot_auth.py:400,465, hermes_cli/web_server.py:2684,17442, cli.py:1873-1899, +] (~130 lines, warm)
23. `shrink` `_coerce_int` clamp helper in 7 copies. One version in utils.py, delete the rest. [mcp_serve.py:215-233, gateway/config.py, tools/file_operations.py, hermes_cli/moa_config.py, hermes_cli/nous_account.py, tools/video_generation_tool.py, tui_gateway/server.py] (~100 lines, warm)
24. `stdlib` `_hook_jsonable` type-dispatch tail reimplements `pydantic_core.to_jsonable_python(value, fallback=str)` (pydantic is a hard dep); keep depth/length bounds + redaction, and import `agent/redact.py:_SENSITIVE_BODY_KEYS` instead of the duplicate set. [run_agent.py:2664-2785] (~65 lines, hot)
25. `shrink` `_configure_provider` vs `_reconfigure_provider`: 105 identical stripped lines incl. two verbatim 15-line entitlement gates. Extract the shared preamble. [hermes_cli/tools_config.py:4142-4328,4655-4814] (~105 lines, warm)
26. `yagni` `InboundPipeline` dynamic-composition surface (`use_before`/`use_after`/`remove`/`when=` + builder) — the single construction site is a flat loop over a hardcoded 20-entry list. [gateway/platforms/yuanbao.py:736-829,3045-3082] (~90 lines, warm)
27. `shrink` Git update-check written twice (banner.py and update_cmd both do shallow-detect + scoped fetch + tip-compare, each commenting that it mirrors the other). One probe helper. [hermes_cli/banner.py:185-330, hermes_cli/update_cmd.py:2192-2360] (~80 lines, warm)
28. `shrink` `_try_refresh_{nous,vertex,copilot}_client_credentials` share one skeleton. One `_refresh_via(resolver, reason)` + 3-entry table; leave codex/env/anthropic alone (genuinely different). [run_agent.py:5390-5700] (~80 lines, warm)
29. `shrink` ~25 `try: <one attribute write> except Exception: pass` guards around statements that can't raise. Delete the guards. [gateway/run.py:9727,9884,10227,10253,13805 + scattered] (~75 lines, mixed)
30. `delete` cli.py `_strip_reasoning_tags` — full duplicate of `agent/agent_runtime_helpers.strip_think_blocks`, self-documented "must stay in sync". Call the shared helper (its unused `agent` param can go too). [cli.py:243-315] (~70 lines, warm)
31. `shrink` `_canonical_model_variants` forward-generates dozens of spellings (dot/dash cross-products × 12 provider × 5 aggregator prefixes) to match override keys. Normalize both sides once and compare. [hermes_constants.py:974-1062] (~70 lines, warm)
32. `shrink` `_format_turn_completion_explanation`: 11-branch `if reason ==` chain returning fixed strings. Dict + two `startswith` fallbacks. [run_agent.py:3581-3677] (~70 lines, warm)
33. `shrink` skills_hub: two parallel elif dispatchers over the same `do_*` set, already drifted (slash path missing 3 actions). One `{action: fn}` table serving both. [hermes_cli/skills_hub.py:1725-2013] (~70 lines, warm)
34. `shrink` Five confirm-prompt implementations with divergent EOF/Ctrl-C semantics + ~20 inline `in {"y","yes"}` sites. `cli_output.prompt_yes_no`, which already exists. [hermes_cli/setup.py:301, hermes_cli/mcp_config.py:59, hermes_cli/checkpoints.py:181, hermes_cli/sessions_cmd.py:50 → hermes_cli/cli_output.py:71] (~70 lines, warm)
35. `delete` `gateway/stream_dispatch.py` — `GatewayEventDispatcher` "seam" only tests import; never wired in. [gateway/stream_dispatch.py:1-132 + tests] (~132 lines, cold)
36. `shrink` `flatten_message_text` re-rolled 5× while `agent/message_content.py:34` is already imported by six modules. [agent/conversation_compression.py:1915, agent/context_compressor.py:908, agent/copilot_acp_client.py:212, mcp_serve.py:234, hermes_cli/session_export.py:224] (~60 lines, warm)
37. `shrink` Three byte-identical async-client shim families (all `asyncio.to_thread(self._sync.create, **kw)`). One generic wrapper. [agent/auxiliary_client.py:1621-1659,1851-1876,1944-1966] (~60 lines, warm)
38. `stdlib` `_get_tool_loop`/`_get_worker_loop` + loop-lifetime half of `_run_async` hand-roll persistent event loops. `asyncio.Runner` (floor is 3.11). [model_tools.py:77-209] (~40 lines, hot)
39. `shrink` `_run_agent` pass-through repeating its 14-param signature and 14-arg call twice, differing only by a context manager. `contextlib.nullcontext()` + `*args`. [gateway/run.py:24626-24677] (~40 lines, hot)
40. `delete` cli.py inline reimplementations of `format_duration_compact`/`format_token_count_compact` — byte-identical to agent/usage_pricing.py:1398,1412; neighboring stubs already lazy-forward there. [cli.py:108-192] (~40 lines, hot)
41. `shrink` Host→default-headers if/elif chain written 3× with drifting branch sets. One `_default_headers_for(base_url, provider)`. [agent/auxiliary_client.py:5651-5686,6044-6065,6314-6343] (~55 lines, warm)
42. `shrink` `_emit_status`/`_emit_warning` and `_buffer_status`/`_buffer_vprint` differ only in a tag string. `_emit(kind, msg)` / `_buffer(kind, msg)`. [run_agent.py:960-1132] (~35 lines, hot)
43. `native` urllib_security.py redirect-credential-scrubbing stack — httpx/requests already strip Authorization on cross-host redirects; keep the stricter allowlist as a ~15-line httpx event hook for its two callers. [hermes_cli/urllib_security.py:1-139] (~100 lines, cold)
44. `stdlib` Four hand-rolled `.env` parsers while python-dotenv is a core dep (env_loader's "avoid importing dotenv" rationale is dead — it imports dotenv at line 12). `dotenv_values()`/`set_key()`. [hermes_cli/env_loader.py:94-111, hermes_cli/main.py:1004-1016,9888-9895, hermes_cli/memory_setup.py:443-466] (~50 lines, warm)
45. `delete` update_cmd module-level `_wait_for_service_active`/`_service_restart_sec` — 0.91-similar duplicates shadowed by the nested originals at all five call sites; zero references. Pure no-op delete. [hermes_cli/update_cmd.py:5462-5555] (~90 lines, cold)
46. `shrink` Sync/async retry twins `_retry_same_provider_{sync,async}` + `_call_fallback_candidate_{sync,async}`. One impl + `asyncio.to_thread`. [agent/auxiliary_client.py:4264-4413,4669-4896] (~90 lines, cold)
47. `delete` Four extra relative-time formatters (same 60/3600/86400 ladder) → `hermes_cli/timefmt.relative_time`, already created as the canonical home. [hermes_cli/checkpoints.py:50, hermes_cli/update_cmd.py:551, hermes_cli/logs.py:384, hermes_cli/kanban_db.py:338] (~45 lines, warm)
48. `stdlib` `_print_summary`: 109 lines of hand-counted box art + hand-rolled median. Row-tuple loop + `statistics.median`. [trajectory_compressor.py:1271-1379] (~85 lines, cold)
49. `yagni` Three hand-rolled TTL caches (dict+lock+monotonic) → one 10-line ttl-memo decorator. [hermes_cli/web_server.py:2964, hermes_cli/nous_account.py:418, hermes_cli/models.py:894] (~40 lines, warm)
50. `stdlib` Two hand-rolled safe-tar extractors → `tarfile.extractall(dest, filter="data")` (hardened on the ≥3.11 floor). [hermes_cli/psutil_android.py:25-64, hermes_cli/profiles.py:1983-2033] (~70 lines, cold)
51. `shrink` Browser-open guard inlined 4× in auth.py — the :8840 copy dropped the remote-session guard its siblings have. One helper in cli_output. [hermes_cli/auth.py:3345,8027,8518,8840] (~32 lines, warm)
52. `shrink` `_create_adapter` 9-branch built-in elif — the `PlatformEntry` registry exists and 22 plugin platforms already use it; the file's own comment says "for now". [gateway/run.py:13959-14038] (~55 lines, cold)
53. `shrink` Three byte-identical "env override → `display.*` config → default True" readers. One `_display_flag(env, key)` (keep blacklist truthiness semantics). [run_agent.py:3457-3989] (~50 lines, cold)
54. `delete` `create_custom_toolset` + `__main__` demo — the demo is its only caller. [toolsets.py:940-1029] (~64 lines, cold)
55. `delete` Statically-false migration gates: `SCHEMA_VERSION == 10` and `SCHEMA_VERSION < 23` when SCHEMA_VERSION is 25 — both blocks unreachable. [hermes_state_schema.py:714-751] (~38 lines, cold)
56. `delete` Dead-code tail, all zero-caller-verified: `SessionDB.restore_rewound` [hermes_state.py:8002-8029], `delete_gateway_routing_entries` [hermes_state.py:3472-3486], `CanonicalUsage`/`estimate_usage_cost` stubs [cli.py:96-106], `_start_cron_ticker` DEPRECATED shim [gateway/run.py:26805-26819], tests-only `intent_ack_continuation_enabled` [agent/agent_runtime_helpers.py:3604-3617], PKCE helpers in triplicate [hermes_cli/auth.py:2859-2876,8338], five dead config keys (`display.copy_shortcut`, `bedrock.discovery.*`, `display.show_cost`) [hermes_cli/config_defaults.py:824-826,1194,1314], empty `_LIGHT_DEFAULT_TERM_PROGRAMS` frozenset guard [cli.py:2560,2705], `backports.zoneinfo` fallback on a 3.11 floor [hermes_time.py:24-28], `_decode_jwt_claims` hand-rolled base64url with PyJWT pinned [hermes_cli/auth.py:2352-2362] (~180 lines, cold)
57. `delete` `tenacity==9.1.4` core pin — zero imports repo-wide; retries use `agent/retry_utils.jittered_backoff`. [pyproject.toml:46] (-1 dep)
58. `yagni` `fire==0.7.1` in core deps — used only by 4 batch/dev runner scripts (one of which is finding 5's delete). argparse, or move to the dev extra. [pyproject.toml, batch_runner.py, trajectory_compressor.py, scripts/sample_and_compress.py] (-1 core dep)
59. `native` Two HTTP clients in core deps: `requests` (37 files) alongside `httpx` (127). Consolidate on httpx; keep the requests pin only if a transitive still needs it. [pyproject.toml:47] (-1 dep, longer-term)

## Verified clean (checked and cleared — don't re-audit)

All 12 built-in + 22 plugin gateway platforms live; agent model adapters/transports all reachable via config; browser/image/video/search/memory/secret-source registries have 3-8 real implementations; the two live compressors are orchestrator-vs-engine, not overlap; web_server.py uses FastAPI idiomatically; env_loader's BOM/UTF-16 hardening is real work; ruamel.yaml earns its keep (comment-preserving `atomic_roundtrip_yaml_update`); utils.py/hermes_logging.py/hermes_bootstrap.py oddities all trace to linked incidents; tool-call JSON repair is issue-backed.

Structural note, not a line item: `GatewayRunner` is 352 methods / ~20.7k lines with four 1.4-2k-line functions — splitting it needs a real design pass (the TurnContext attempt, finding 13, shows extraction plumbing alone makes it worse).

Coverage caveats: cli.py's interior (~4.2k-18k) and hermes_state.py got pattern-targeted, not exhaustive, passes.

`net: ~-9,700 lines, -2 deps now (tenacity, fire), -1 more (requests) after consolidation.`
